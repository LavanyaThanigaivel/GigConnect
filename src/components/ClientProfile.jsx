import React from "react";

export default function ClientProfile() {
  return (
    <div style={{ padding: "2rem" }}>
      <div className="profile-card">
        <h2>Client Profile (Minimal)</h2>
        <p className="muted">This is a placeholder client profile for the GigConnect demo frontend.</p>
      </div>
    </div>
  );
}
